# 当前版本真实截图与验收清单

状态：v0.4 自动化面板证据已完成。图片位于 `docs/images/workflow/v0.4/`，哈希和源报告记录于
`artifacts/host-validation/m4/panel-evidence-v0.4.0-dev9.json`。

截图必须来自当前打包版本的真实 Unreal Editor 会话。离线 fixture、UI 草图、旧版截图和合成图不能代替以下证据。

| 编号 | 场景 | 画面必须包含 | 证明内容 |
| --- | --- | --- | --- |
| 01 | 空状态 | 内置 Profile、阈值摘要、空列表与禁用运行按钮 | 首次打开不会误触发扫描 |
| 02 | 资产总览 | 通过、需处理、失败混合；三角形/顶点/材质槽/LOD/Nanite 列 | 完整台账不会隐藏通过资产 |
| 03 | 通过筛选 | 15 个通过资产 | 插件不是只会制造红色告警 |
| 04 | 待处理筛选 | 9 个需处理资产及各自问题数 | 资产级处理范围清晰 |
| 05 | 问题明细 | 错误、警告、实测值、阈值和中文证据说明 | 每条结论可追溯到 Profile |
| 06 | 三角形证据 | 三角形预算的 9 条过滤结果 | 中文关键字可以搜索规则与说明 |
| 07 | 材质槽证据 | 3 条材质槽告警 | 不同严重度和规则可以独立复核 |
| 08 | 失败隔离 | Material 与缺失路径两条失败 | 单资产失败不会拖垮整批 |

## 截图验收

- 统一使用 16:9 或接近 16:9 的清晰画面，面板文字可读；
- 不出现个人账号、聊天窗口、密钥、无关工程路径或其他项目内容；
- 截图文件放入 `docs/images/workflow/v0.4/`，名称使用 `01-` 至 `08-` 的稳定前缀；
- README 只引用已经真实拍摄并核对当前版本的图片；
- 每次 UI 大改后重新拍摄，不沿用无法证明当前行为的旧图。

## 仍需人工录屏覆盖的交互

自动化截图不声明鼠标操作已经通过。正式录屏仍应展示 Content Browser 多选、Profile 下拉切换、
“读取当前选择”“开始只读审计”“打开最新报告”以及外部 JSON 查看过程。
